// use shmr::{Bucket, PoolMap};
// use std::collections::HashMap;
// use std::path::PathBuf;
// use sysinfo::Disks;

fn main() {
    // let mut bucket = HashMap::new();
    // bucket.insert("d0".to_string(), Bucket::new(PathBuf::from("/")));
    //
    // let mut buckets: PoolMap = HashMap::new();
    // buckets.insert("pool0".to_string(), bucket);
    //
    // let disks = Disks::new_with_refreshed_list();
    //
    // for (_, bucket_map) in buckets.iter_mut() {
    //     for (_, bucket) in bucket_map.iter_mut() {
    //         bucket.update(&disks);
    //     }
    // }
    //
    // println!("{:#?}", buckets);
}
